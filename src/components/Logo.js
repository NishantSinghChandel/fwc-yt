export default function Logo() {
  return (
    <img
      className="rounded-full fixed top-[10px] right-[20px] "
      src="logo.PNG"
      alt="logo"
      height={150}
      width={150}
    />
  );
}
